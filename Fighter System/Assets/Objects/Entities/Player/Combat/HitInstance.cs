public struct HitInstance
{
    public readonly int hitPause;
    public readonly int damage;

    public HitInstance(int hitPause, int damage)
    {
        this.hitPause   = hitPause;
        this.damage     = damage;
    }
}
